Это проект для изучения Git и Cmake.
Пока мы создали только один файл: README.txt.
Он пока находится в, надеюсь созданной по умолчанию, ветке MASTER.

Создадим ветку ARGS. В ней перепишем программу таким образом, чтобы она складывала аргументы, полученные из командной строки. Опции программы будем обрабатывать с помощью библиотеки Boost. Таким образом в новой ветке изменится еще и CMakeLists.txt.
